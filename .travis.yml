dist: trusty
sudo: false

language: node_js
node_js:
  - '8'

cache:
  directories:
    - .naonedia-app/node_modules

install:
  - cd naonedia-app
  - npm install

script:
  - npm run lint
  - ng build --prod --output-path ../nginx/www


deploy:
  provider: script
  script: echo "deploy"
  on:
    branch: master